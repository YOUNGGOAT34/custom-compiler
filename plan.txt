lexer/tokenizer-takes the characters written,and convert them to  certain tokens
parser-Goes through the tokens generated by the lexer and make sure they work Syntactically
code generator-takes everything from the parser and turns it to assembly


tokens:
     keyword
     separator
     literal

     example: exit(1);  keyword-exit,separator-(), literal-1